{
	"name": "rbxts-transformer-runtime-imports",
	"version": "1.2.0",
	"description": "A transformer that allows imports to only be called if the runtime of the code is on the server or the client.",
	"main": "out/index.js",
	"license": "ISC",
	"dependencies": {
		"typescript": "4.6.2"
	},
	"scripts": {
		"build": "tsc",
		"watch": "tsc -w",
		"test": "tsc -p testFiles.tsconfig.json",
		"prepublishOnly": "npm run build"
	},
	"devDependencies": {
		"@types/node": "^14.14.11",
		"@typescript-eslint/eslint-plugin": "^4.9.1",
		"@typescript-eslint/parser": "^4.9.1",
		"eslint": "^7.15.0",
		"eslint-config-prettier": "^7.0.0",
		"eslint-plugin-prettier": "^3.2.0",
		"prettier": "^2.2.1",
		"ts-expose-internals": "^4.5.4",
		"ts-transformer-testing-library": "^1.0.0-alpha.7"
	},
	"files": [
		"out"
	]
}
